# GitHub Pages 加密部署指南

## 🔒 代码保护方案

本项目使用 **JavaScript 代码混淆** 技术来保护源代码：

1. **本地开发**：保留原始源代码，方便开发和调试
2. **GitHub 部署**：自动混淆构建后的 JavaScript 文件
3. **GitHub Pages 访问**：混淆后的代码仍可正常运行
4. **源代码保护**：即使有人下载仓库，也无法直接使用混淆后的代码

## 📋 部署流程

### 1. 安装依赖

```bash
npm install
```

### 2. 本地测试混淆构建

```bash
# 构建并混淆
npm run build:obfuscated

# 预览混淆后的效果
npm run preview
```

### 3. 部署到 GitHub Pages

#### 方式一：使用 GitHub Actions（推荐）

1. 确保仓库已设置为**公开**（GitHub Pages 要求）
2. 推送代码到 GitHub：
   ```bash
   git add .
   git commit -m "Deploy with obfuscated code"
   git push origin master
   ```
3. GitHub Actions 会自动：
   - 构建项目
   - 混淆所有 JavaScript 文件
   - 部署到 GitHub Pages

#### 方式二：手动部署

```bash
# 1. 构建并混淆
npm run build:obfuscated

# 2. 切换到 gh-pages 分支（或使用 GitHub Actions）
git checkout -b gh-pages
git add dist/
git commit -m "Deploy obfuscated build"
git push origin gh-pages
```

## 🔐 混淆配置说明

混淆脚本位于 `scripts/obfuscate-build.ts`，使用以下保护措施：

- ✅ **控制流扁平化**：使代码逻辑难以理解
- ✅ **死代码注入**：添加无用代码干扰逆向
- ✅ **字符串数组编码**：使用 Base64 编码字符串
- ✅ **标识符混淆**：变量名使用十六进制
- ✅ **自防御**：防止代码被修改
- ✅ **字符串分割**：拆分长字符串
- ✅ **对象键转换**：混淆对象属性名

## ⚠️ 重要注意事项

1. **源代码保护**：
   - 本地源代码不会被提交到 GitHub（通过 .gitignore 保护）
   - 只有混淆后的构建文件会被部署

2. **API 密钥**：
   - 确保敏感信息（如 API 密钥）使用环境变量
   - 不要硬编码在源代码中

3. **调试**：
   - 混淆后的代码难以调试
   - 开发时使用 `npm run dev`（不混淆）
   - 部署前使用 `npm run build:obfuscated` 测试

4. **性能影响**：
   - 混淆会增加文件大小（约 20-30%）
   - 首次加载可能稍慢，但运行性能不受影响

## 🛠️ 故障排除

### 问题：混淆后页面无法运行

**解决方案**：
1. 检查浏览器控制台错误
2. 确保 `debugProtection` 设置为 `false`
3. 确保 `disableConsoleOutput` 设置为 `false`

### 问题：混淆文件太大

**解决方案**：
1. 调整 `deadCodeInjectionThreshold` 降低死代码注入
2. 调整 `stringArrayThreshold` 降低字符串数组使用

### 问题：GitHub Actions 部署失败

**解决方案**：
1. 检查 Actions 日志
2. 确保 `javascript-obfuscator` 已安装
3. 确保 Node.js 版本 >= 20

## 📝 文件说明

- `scripts/obfuscate-build.ts` - 混淆脚本
- `.github/workflows/deploy-gh-pages.yml` - GitHub Actions 工作流
- `package.json` - 包含 `build:obfuscated` 和 `obfuscate` 脚本

## 🔗 相关链接

- [JavaScript Obfuscator 文档](https://github.com/javascript-obfuscator/javascript-obfuscator)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

